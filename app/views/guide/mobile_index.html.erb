<h1>SPORT ON TV</h1>
<h2>EVERY SPORT, EVERY CHANNEL</h2>
<% if !@regions.nil? %>
<ul>
    <li><h3>Select Region</h3></li>
    <% @regions.each do |region| %>
    <li>
    <%= link_to region.name, URI.escape("/"+region.name+"/") %>
    </li>
    <% end %>
</ul>
<% end %>